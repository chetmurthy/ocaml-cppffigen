(
 (stanzas
 (
(CPP PROLOGUE
   "
#include <stdio.h>
#include <string>
#include <unistd.h>
#include <ostream>
#include <iostream>
#include <vector>

#include \"somecode.h\"
#include \"cppffi.h\"
")
  (TYPEDEF
  ((mltype (EXP int64))
   (cpptype (ID size_t))
   (name size_t)))
 
   (FOREIGN
    ((ID std::string))
    somecode_foo
    (((PRIM INT) n))
     "_res0 = somecode::foo(n); ")

   (FOREIGN
    ((PRIM INT))
    somecode_int32_to_int
    (((PRIM INT) n))
     "_res0 = n; ")

   )
  )
 )
